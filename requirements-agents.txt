# Host-side telemetry collectors; Windows-only dependencies are platform marked.
requests>=2.32,<3
watchdog>=4,<7
psutil>=5.9,<8
pywin32>=306; sys_platform == "win32"
wmi>=1.5; sys_platform == "win32"
